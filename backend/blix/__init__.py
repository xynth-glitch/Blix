"""Blix backend — AI-powered public transport assistant.

The backend is organized around a *deterministic transport engine*. The AI layer
(added later) is only a consumer of this engine and never a source of transit facts.
"""

__version__ = "0.1.0"
